kind: TestDefinition
metadata:
  name: mcm-oot-opnestack-integration-test
spec:
  description: Tests the deployment and functionality of MCM OpenStack extension.

  activeDeadlineSeconds: 1200
  labels: ["playground"]

  command: [bash, -c]
  args:
  - >-
    make MC_IMAGE_TAG=$(shell cat VERSION) CONTROL_KUBECONFIG=${TM_KUBECONFIG_PATH}/seed.config TARGET_KUBECONFIG=${TM_KUBECONFIG_PATH}/shoot.config test-integration 
  image: eu.gcr.io/gardener-project/cc/job-image-golang:0.11.0